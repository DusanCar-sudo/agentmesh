# Dream — 2026-07-13

38 episodes recalled (categories: other)

## Lessons

- **`src/utils/pagination.js` off-by-one error is a persistent trap**: Attempted ~20 times across deepseek-v4-pro and mimo-v2.5-pro without a lasting fix. The test suite at `src/utils/pagination.test.js` defines expected behavior, but models keep producing fixes that don't pass or revert. This bug resists automated fixing.
- **`src/data-structures/lru-cache.js`**: get() fails to update access order, causing incorrect eviction. Must maintain O(1) for both get and put.
- **`src/ui/sanitize-html.js`**: Must escape all dangerous characters AND strip event handler attributes (onload, onclick, etc.) for safe DOM rendering.
- **`src/db/build-user-query.js`**: Vulnerable to SQL injection via string concatenation — must parameterize.
- **`src/middleware/rate-limiter.js`**: Two bugs — allows burst exceeding maxRequests, and leaks memory by never cleaning expired entries.
- **`src/testing/mock-fetch.js`**: Leaks global state between tests and silently ignores unexpected URLs instead of throwing.
- **`src/ci/validate-dockerfile.js`**: Missing checks for pinned base-image versions, secret leakage via COPY/ADD, and running as root.
- **Anthropic OAuth PKCE port**: `~/.hermes/hermes-agent/agent/anthropic_adapter.py` and `transports/anthropic.py` implement PKCE flow in Python. A TypeScript port was completed by deepseek-v4-pro (318s task, approved).

## Patterns

- **deepseek-v4-pro loops on pagination.js**: ~18 consecutive attempts on the same off-by-one bug (July 1–2), all approved but apparently not persisting. However, it successfully completed a complex 318s porting task (Python→TS OAuth PKCE). Strong at porting, weak at certain edge-case bug fixes.
- **mimo-v2.5-pro handles security/correctness bug fixes reliably**: All 6 distinct bug fix tasks (SQL injection, XSS, rate limiter, Dockerfile validator, mock fetch, LRU cache) were completed and approved on July 2, each taking 38–153s. It also handled the same batch again (likely re-runs), again successfully.
- **Batch re-runs of the same tasks**: The exact same 6 bug fixes were dispatched twice on July 2 (~10:38 and ~11:17), suggesting a testing/reliability-validation loop.
- **zhipu/glm-5.2 tasks not approved**: Three "say hi" tasks were handled but marked NOT approved — likely used for smoke testing rather than real work.
- **`:graph` and `:graphify` commands**: System maintenance tasks handled by mimo-v2.5-pro, taking 153–162s.

## Open threads

- **pagination.js is still broken**: Despite ~20 attempts across two models, the off-by-one error in `src/utils/pagination.js` does not appear to have a verified fix. This needs a fundamentally different approach or manual attention.
- **Anthropic OAuth TypeScript port status**: Completed but no follow-up verification episodes visible — may need integration testing.
- **Graph data integrity**: `:graph refresh` and `:graphify` were run — unclear if the graph is now in a clean state or if further work is needed.

## Tomorrow brief

The `src/utils/pagination.js` off-by-one error has been attempted ~20 times without a durable fix — do not retry the same approach; read `src/utils/pagination.test.js` carefully and consider a manual rewrite. mimo-v2.5-pro reliably handles security/correctness bug fixes (XSS, SQL injection, LRU cache, rate limiter, Dockerfile validation, mock fetch) in ~40–150s each. deepseek-v4-pro excels at complex porting tasks (Python→TS OAuth PKCE, 318s) but gets stuck in loops on certain edge-case bugs. The `~/.hermes/hermes-agent/` Anthropic PKCE TypeScript port was completed but needs integration verification.
